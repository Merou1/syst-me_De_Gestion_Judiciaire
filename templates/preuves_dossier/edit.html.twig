{% extends 'base.html.twig' %}

{% block title %}Edit Preuves Dossier{% endblock %}

{% block body %}
    <h1>Edit Preuves Dossier</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}

    <a href="{{ path('app_preuves_dossier_index') }}">back to list</a>
{% endblock %}
