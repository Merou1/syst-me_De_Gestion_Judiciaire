{% extends 'base.html.twig' %}

{% block title %}Preuves Dossier{% endblock %}

{% block body %}
    <h1>Preuves Dossier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ preuvesDossier.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ preuvesDossier.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ preuvesDossier.description }}</td>
            </tr>
            <tr>
                <th>Dossier</th>
                <td>{{ preuvesDossier.dossier.numeroDossier }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_preuves_dossier_index') }}">back to list</a>

    {{ include('preuves_dossier/_delete_form.html.twig') }}
{% endblock %}
